services:
  nginx:
    image: nginx:1.29
    container_name: nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/certs:/etc/nginx/certs
    networks:
      - cicd-net
    depends_on:
      - jenkins
        
  jenkins:
    build:
      context: ./jenkins
      dockerfile: docker/Dockerfile
    hostname: jenkins.example.com
    container_name: jenkins
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - jenkins_home:/var/jenkins_home
    networks:
      - cicd-net
    depends_on:
      - ldap

  nexus:
    build:
      context: ./nexus
      dockerfile: docker/Dockerfile
    hostname: nexus.example.com
    container_name: nexus
    restart: unless-stopped
    env_file:
      - .env
    environment:
      INSTALL4J_ADD_VM_PARAMS: ${INSTALL4J_ADD_VM_PARAMS}
    volumes:
      - nexus_data:/nexus-data
    networks:
      - cicd-net
    depends_on:
      - ldap

  nexus-init:
    image: alpine:3.23
    env_file:
      - .env
    entrypoint: >
      sh -c "
        apk add --no-cache curl jq gettext &&
        chmod +x /scripts/*.sh &&
        /scripts/provision.sh
      "
    volumes:
      - ./nexus/scripts:/scripts
      - ./nexus/templates:/templates
      - nexus_data:/nexus-data
    networks:
      - cicd-net
    depends_on:
      - nexus

  rundeck:
    build:
      context: ./rundeck
      dockerfile: docker/Dockerfile
    hostname: rundeck.example.com
    container_name: rundeck
    restart: unless-stopped
    environment:
      RUNDECK_GRAILS_URL: ${RUNDECK_GRAILS_URL}
      RUNDECK_SERVER_FORWARDED: "true"
      RUNDECK_JAAS_MODULES_0: JettyCombinedLdapLoginModule
      RUNDECK_JAAS_LDAP_PROVIDERURL: ldaps://${LDAP_HOST}:${LDAPS_PORT}
      RUNDECK_JAAS_LDAP_BINDDN: ${LDAP_BIND_DN}
      RUNDECK_JAAS_LDAP_BINDPASSWORD: ${LDAP_BIND_PASSWORD}
      RUNDECK_JAAS_LDAP_USERBASEDN: ${LDAP_USER_BASE},${LDAP_BASE_DN}
      RUNDECK_JAAS_LDAP_ROLEBASEDN: ${LDAP_ROLE_BASE},${LDAP_BASE_DN}
    volumes:
      - rundeck_data:/home/rundeck/server/data
    networks:
      - cicd-net
    depends_on:
      - ldap

  svn:
    build: 
      context: ./svn
      dockerfile: docker/Dockerfile
    hostname: svn.example.com
    container_name: svn
    restart: unless-stopped
    env_file:
      - .env 
    volumes:
      - ./svn/repos:/var/svn/repos
      - ./svn/seed:/seed
      - ./svn/hooks:/hooks
    networks:
      - cicd-net
    depends_on:
      - ldap

  ldap:
    container_name: ldap
    image: osixia/openldap:1.5.0
    environment:
      LDAP_ORGANISATION: ${LDAP_ORGANISATION}
      LDAP_DOMAIN: ${LDAP_DOMAIN}
      LDAP_ADMIN_PASSWORD: ${LDAP_BIND_PASSWORD}
      LDAP_TLS: true
      LDAP_TLS_CRT_FILENAME: ${LDAP_TLS_CRT_FILENAME}
      LDAP_TLS_KEY_FILENAME: ${LDAP_TLS_KEY_FILENAME}
      LDAP_TLS_CA_CRT_FILENAME: ${LDAP_TLS_CA_CRT_FILENAME}
      LDAP_TLS_VERIFY_CLIENT: never
    volumes:
      - ldap_conf:/container/service/slapd
      - ldap_data:/var/lib/ldap
      - ./ldap/ldif:/container/service/slapd/assets/config/bootstrap/ldif/custom
      - ./ldap/certs:/container/service/slapd/assets/certs
    ports:
      - "389:389"
      - "636:636"
    command: --copy-service
    networks:
      - cicd-net

  ldapadmin:
    container_name: ldapadmin
    image: osixia/phpldapadmin:0.9.0
    environment:
      PHPLDAPADMIN_LDAP_HOSTS: ldap
      PHPLDAPADMIN_HTTPS: false
    volumes:
      - ldapadmin_conf:/container/service/phpldapadmin
    ports:
      - "8080:80"
    networks:
      - cicd-net
    depends_on:
      - ldap

  mailhog:
    container_name: mailhog
    image: mailhog/mailhog:v1.0.1
    volumes:
      - mailhog_data:/data
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - cicd-net

# ── 네트워크 ──────────────────────────────────────────────────────────────────
networks:
  cicd-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/24

# ── 볼륨 ──────────────────────────────────────────────────────────────────────
volumes:
  jenkins_home:
  nexus_data:
  rundeck_data:
  svn_repos:
  ldap_conf:
  ldap_data:
  ldapadmin_conf:
  mailhog_data:
